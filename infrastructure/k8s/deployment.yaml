apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-service
  template:
    metadata:
      labels:
        app: todo-service
    spec:
      containers:
      - name: todo-service
        image: <ACR_LOGIN_SERVER>/todo-app:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          # Replace <postgres_fqdn> with the FQDN provided by the PostgreSQL resource
          value: "postgresql://dbadmin:YourSecurePassword123!@<postgres_fqdn>:5432/tododb"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          # Direct telemetry to the OpenTelemetry collector service deployed below
          value: "http://otel-collector:4317"
        resources:
          requests:
            memory: "128Mi"
            cpu: "250m"
          limits:
            memory: "256Mi"
            cpu: "500m"